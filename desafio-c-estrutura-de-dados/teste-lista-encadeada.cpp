#include <stdio.h>
#include <stdlib.h>

typedef struct{
	int a;
	struct NODO *proxima;
}NODO;

int main(){
	//NODO *lista = malloc(sizeof(LISTA));	//recebe o endereço de um espaço na memória
	
	lista->a = 0;
	
	printf("%i\n", lista->a);
	
	
	
	return 0;
}
